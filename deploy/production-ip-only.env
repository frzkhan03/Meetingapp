# PyTalk Production Environment - IP Only (No Domain)
# Use this when testing with EC2 public IP before setting up a domain
# Copy to /opt/pytalk/backend/.env

# ==================== CORE SETTINGS ====================
# IMPORTANT: Generate a new secret key
SECRET_KEY=CHANGE_ME_generate_with_python_secrets

# For IP-only access, keep PRODUCTION=False to disable SSL redirect
# Change to True after setting up SSL with a domain
DEBUG=False
PRODUCTION=False

# Your EC2 public IP
ALLOWED_HOSTS=localhost,127.0.0.1,52.66.26.150

# ==================== DATABASE ====================
DB_NAME=pytalk
DB_USER=pytalk
DB_PASSWORD=CHANGE_ME_secure_password
DB_HOST=localhost
DB_PORT=5432

# ==================== REDIS ====================
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# ==================== EMAIL ====================
MAIL_USER=your-email@gmail.com
MAIL_PASS=your-app-password

# ==================== SECURITY ====================
ENCRYPTION_KEY=CHANGE_ME_generate_encryption_key
ADMIN_URL=pytalk-admin-2024/

# For IP-only access (http://)
CSRF_TRUSTED_ORIGINS=http://52.66.26.150,http://localhost
WEBSOCKET_ALLOWED_ORIGINS=http://52.66.26.150,ws://52.66.26.150,http://localhost,ws://localhost

# ==================== AWS S3 (Recording Storage) ====================
AWS_ACCESS_KEY_ID=CHANGE_ME_your_access_key
AWS_SECRET_ACCESS_KEY=CHANGE_ME_your_secret_key
AWS_S3_BUCKET_NAME=pytalk-recordings
AWS_S3_REGION=ap-south-1

# ==================== RATE LIMITING ====================
RATE_LIMIT_LOGIN_ATTEMPTS=5
RATE_LIMIT_LOGIN_WINDOW=300
