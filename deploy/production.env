# PyTalk Production Environment Configuration
# Copy this file to /opt/pytalk/backend/.env

# ==================== CORE SETTINGS ====================
# IMPORTANT: Generate a new secret key with: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=CHANGE_ME_generate_a_secure_random_key

# Production mode - MUST be set correctly
DEBUG=False
PRODUCTION=True

# Your domain (replace with your actual domain or EC2 public IP)
ALLOWED_HOSTS=your-domain.com,www.your-domain.com,your-ec2-public-ip

# ==================== DATABASE ====================
# Option 1: Local PostgreSQL (requires ~200MB RAM)
DB_NAME=pytalk
DB_USER=pytalk
DB_PASSWORD=CHANGE_ME_secure_database_password
DB_HOST=localhost
DB_PORT=5432

# Option 2: AWS RDS (recommended for t3.micro to save RAM)
# DB_HOST=your-rds-endpoint.region.rds.amazonaws.com

# ==================== REDIS ====================
# Local Redis (requires ~50-100MB RAM)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Or use AWS ElastiCache (recommended for t3.micro)
# REDIS_HOST=your-elasticache-endpoint.region.cache.amazonaws.com

# ==================== EMAIL ====================
# Gmail SMTP (create an App Password in Google Account settings)
MAIL_USER=your-email@gmail.com
MAIL_PASS=your-gmail-app-password

# ==================== SECURITY ====================
# IMPORTANT: Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
ENCRYPTION_KEY=CHANGE_ME_generate_encryption_key

# Custom admin URL (harder to guess = more secure)
ADMIN_URL=pytalk-secure-admin-2024/

# CSRF trusted origins (MUST include https://)
CSRF_TRUSTED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# WebSocket allowed origins (MUST include wss:// for production)
WEBSOCKET_ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com,wss://your-domain.com,wss://www.your-domain.com

# ==================== RATE LIMITING ====================
RATE_LIMIT_LOGIN_ATTEMPTS=5
RATE_LIMIT_LOGIN_WINDOW=300
