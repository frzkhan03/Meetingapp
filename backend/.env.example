# PyTalk Environment Configuration
# Copy this file to .env and update the values

# ==================== CORE SETTINGS ====================
# Generate a strong secret key for production: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=your-super-secret-key-here

# Set to False in production
DEBUG=True

# Set to True in production for HTTPS enforcement
PRODUCTION=False

# Comma-separated list of allowed hosts
ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com

# ==================== DATABASE ====================
DB_NAME=PyTalk
DB_USER=postgres
DB_PASSWORD=your-secure-database-password
DB_HOST=localhost
DB_PORT=5432

# ==================== REDIS (required for production) ====================
REDIS_HOST=localhost
REDIS_PORT=6379

# ==================== EMAIL ====================
MAIL_USER=your-email@gmail.com
MAIL_PASS=your-app-specific-password

# ==================== SECURITY ====================
# Encryption key for sensitive data (generate with: python -c "import secrets; print(secrets.token_urlsafe(32))")
ENCRYPTION_KEY=your-encryption-key-here

# Custom admin URL (change from default 'admin/' for security)
ADMIN_URL=secure-admin-panel/

# CSRF trusted origins (comma-separated)
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000,https://yourdomain.com

# WebSocket allowed origins (comma-separated)
WEBSOCKET_ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000,ws://localhost:8000,wss://yourdomain.com

# ==================== RATE LIMITING ====================
# Maximum login attempts before rate limiting
RATE_LIMIT_LOGIN_ATTEMPTS=5
# Rate limit window in seconds
RATE_LIMIT_LOGIN_WINDOW=300

# ==================== PRODUCTION CHECKLIST ====================
# Before deploying to production, ensure:
# [ ] SECRET_KEY is a unique, random value
# [ ] DEBUG=False
# [ ] PRODUCTION=True
# [ ] ALLOWED_HOSTS includes your domain
# [ ] Database credentials are secure
# [ ] ENCRYPTION_KEY is set and backed up securely
# [ ] HTTPS is configured
# [ ] CSRF_TRUSTED_ORIGINS includes your domain (with https://)
# [ ] WEBSOCKET_ALLOWED_ORIGINS includes your domain (with wss://)
# [ ] Redis is configured and running
# [ ] Email settings are configured
# [ ] Log files are being monitored
# [ ] Run: python manage.py collectstatic
# [ ] Run: python manage.py migrate
