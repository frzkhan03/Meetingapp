django>=4.2
channels>=4.0
channels-redis>=4.1  # Redis channel layer for production
daphne>=4.0
celery>=5.3  # Async task queue for background DB operations
psycopg2-binary>=2.9
python-dotenv>=1.0
whitenoise>=6.6  # Static file serving in production
django-redis>=5.4  # Redis cache backend for sessions, caching, rate limiting

# AWS
boto3>=1.28  # AWS SDK for S3 recording storage

# Payments
requests>=2.31  # HTTP client for PayU API calls

# Security packages
argon2-cffi>=21.0  # Argon2 password hashing (most secure)
cryptography>=41.0  # Encryption library for data at rest
bleach>=6.0  # HTML sanitization
